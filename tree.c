void createTree(int indexBegin) {
  for(int i = 0;i<255;i++) {
    if temAlgoNaFat[i] 
      criaNodo
        
  }
}

void unorderedTree() {
  // Passa pela fat e verifica se o indice está fora de ordem.
  // Caso esteja,  
}

typedef struct s_Node {
  int name[30];
  int indexPosition;
  struct s_List* childs;
} Node;

typedef struct s_List {
  struct s_Node* nextNode;
} NodeList;
